# Team Flow

## Models

The `Teams` model (`backend/models/Teams.py:6`) maps to the `teams` table. It has `team_id` (Integer PK, line 8), `team_name` (String, indexed, not null, line 9), `team_size` (Integer, nullable, line 10), `description` (Text, nullable, line 11), `created_at` (DateTime with timezone, defaulting to `datetime.now(UTC)`, line 12), and `org_id` (Integer FK to `organization.organization_id`, not null, line 13). The model defines relationships: `organization` back-populates `teams` (line 15), `channels` with `cascade="all, delete-orphan"` (line 16), `team_members` to `Team_association` with cascade (line 17), `team_roles` with cascade (line 18), `files` with cascade (line 19), `tasks` with cascade (line 20), and `users` as a many-to-many via the `team_association` secondary table with `viewonly=True` back-populating `teams` (line 21).

The `Team_association` model (`backend/models/Team_association.py:6`) maps to `team_association`. It has a composite primary key of `team_id` (Integer FK→`teams.team_id`, primary_key=True, nullable=False, line 8) and `user_id` (Integer FK→`users.user_id`, primary_key=True, nullable=False, line 9). It defines `team` back-populating `team_members` (line 11) and `user` back-populating `team_associations` (line 12).

The `Team_roles` model (`backend/models/Team_roles.py:6`) maps to `team_roles`. It has `team_role_id` (Integer PK, line 8), `user_id` (Integer FK→`users.user_id`, nullable=False, line 9), `team_id` (Integer FK→`teams.team_id`, nullable=False, line 10), `role` (String, nullable=False, line 11), and seven boolean permission fields each defaulting to `False`: `can_create_channels` (line 12), `can_send_messages` (line 13), `can_delete_messages` (line 14), `can_manage_roles` (line 15), `can_kick_members` (line 16), `can_make_announcement` (line 17), and `can_manage_tasks` (line 18). It also has `created_at` (DateTime with timezone, default `datetime.now(UTC)`, line 19), `updated_at` (DateTime with timezone, default `datetime.now(UTC)` with `onupdate=datetime.now(UTC)`, line 20), and relationships for `user` back-populating `team_roles` (line 22) and `team` back-populating `team_roles` (line 23).

## Router Endpoints

`backend/routers/team_router.py:28` defines `router = APIRouter()`. Lines 1-20 import `Add_members_team`, `Update_team_member_role`, `Channels_input`, and all 15 service functions.

`POST /organization/{org_id}/create_team` at line 31 accepts a `team_creation` body, depends on `current_user` and `connect_databse`, and calls `create_team(data, user, db)` (lines 31-38).

`GET /organization/{org_id}/teams` at line 41 calls `fetch_teams_service(org_id, user, db)` (lines 41-47).

`PUT /team/{team_id}` at line 50 accepts a `team_creation` body and calls `update_team_service(team_id, data, user, db)` (lines 50-57).

`DELETE /team/{team_id}` at line 60 calls `delete_team_service(team_id, user, db)` (lines 60-66).

`POST /team/{team_id}` at line 69 accepts an `Add_members_team` body and calls `add_memebers_to_teams(team_id, data, user, db)` (lines 69-76).

`GET /team/{team_id}/members` at line 79 calls `fetch_team_members_service(team_id, user, db)` (lines 79-85).

`PUT /team/{team_id}/member/{member_user_id}/permissions` at line 88 accepts an `Update_team_member_role` body and calls `update_member_permissions_service(team_id, member_user_id, data, user, db)` (lines 88-96).

`PUT /team/{team_id}/member/{member_user_id}/revoke-permissions` at line 99 accepts an optional `permission_name` query parameter (`str | None = Query(None)`) and calls `revoke_permissions_from_team_memebers(team_id, member_user_id, user, db, permission_name)` (lines 99-107).

`DELETE /team/{team_id}/member/{member_user_id}` at line 110 calls `kick_member_service(team_id, member_user_id, user, db)` (lines 110-117).

`GET /user/teams` at line 120 calls `fetch_user_team_service(user, db)` (lines 120-125).

`POST /organization/{org_id}/team/{team_id}/channels` at line 128 accepts a `Channels_input` body and calls `create_channels_for_teams_service(org_id, team_id, data, user, db)` (lines 128-136).

`GET /organization/{org_id}/team/{team_id}/channels` at line 139 calls `fetch_channels_for_teams_service(org_id, team_id, user, db)` (lines 139-146).

`GET /organization/{org_id}/team/{team_id}/member/{user_id}` at line 149 calls `fetch_members_info(org_id, team_id, user_id, user, db)` (lines 149-157).

`GET /organization/{org_id}/team/{team_id}/channel/{channel_id}/files` at line 160 calls `fetch_files_for_team_channel_service(org_id, team_id, channel_id, user, db)` (lines 160-168).

`GET /organization/{org_id}/team/{team_id}/file/{file_id}/content` at line 171 calls `view_pdf(org_id, team_id, file_id, user, db)` (lines 171-179).

## Service Functions

### Create Team

`create_team(data, user, db)` at `backend/services/team_service.py:23` starts by extracting `user_id = user.user_id` (line 24). It queries `Organization` by `data.org_id` — if not found, raises 404 "Organization not found" (lines 26-29). It checks if the requester is the org owner via `db.query(Organization).filter(Organization.organization_id == data.org_id, Organization.owner_id == user_id).first()` (lines 31-34) and if they are an org admin via `db.query(Organization_members).filter(Organization_members.org_id == data.org_id, Organization_members.memmber_id == user_id, Organization_members.role_user == "ADMIN").first()` (lines 36-40). If neither, raises 403 "Only organization owner or admin can create teams" (lines 42-46). It checks for a duplicate team name with `db.query(Teams).filter(Teams.org_id == data.org_id, Teams.team_name == data.team_name).first()` — if found, raises 400 "Team name already exists in this organization" (lines 48-54). Creates a `Teams(team_name=data.team_name, description=data.description, org_id=data.org_id)`, adds, commits, refreshes (lines 56-64). Calls `create_log(db, org_id=data.org_id, actor_id=user_id, action="team_created", target_id=new_team.team_id, target_type="team", metadata={"team_name": new_team.team_name})` (line 66). Returns `{"message": "Team created successfully", "team_id": new_team.team_id, "team_name": new_team.team_name, "description": new_team.description, "org_id": new_team.org_id, "created_at": new_team.created_at}` (lines 68-75).

### Fetch Teams

`fetch_teams_service(org_id, user, db)` at line 77 extracts `user_id = user.user_id` (line 78). Queries `Organization` by ID — if not found, raises 404 "Organization not found" (lines 80-83). Checks `is_owner = found_organization.owner_id == user_id` (line 85). Queries `Organization_members` for membership — if neither owner nor member, raises 403 "You must be a member of this organization to view teams" (lines 87-93). Queries `Teams` filtered by `org_id`, returning a list of `{"team_id", "team_name", "description", "org_id", "created_at"}` for each team (lines 95-108).

### Delete Team

`delete_team_service(team_id, user, db)` at line 110 extracts `user_id` (line 111). Queries `Teams` by ID — if not found, raises 404 "Team not found" (lines 113-116). Queries the team's org — if not found, raises 404 "Organization not found" (lines 118-121). Checks `is_owner` and `is_admin` — if neither, raises 403 "Only organization owner or admin can delete teams" (lines 123-134). Creates a log entry before deletion with `create_log(db, org_id=team.org_id, actor_id=user_id, action="team_deleted", target_id=team_id, target_type="team", metadata={"team_name": team.team_name})` (line 136). Deletes the team via `db.delete(team)`, commits (lines 138-139). Returns `{"message": "Team deleted successfully", "team_id": team_id}` (lines 141-144).

### Update Team

`update_team_service(team_id, data, user, db)` at line 146 extracts `user_id` (line 147). Queries `Teams` by ID — if not found, raises 404 "Team not found" (lines 149-152). Queries the team's org — if not found, raises 404 "Organization not found" (lines 154-157). Checks owner and admin — if neither, raises 403 "Only organization owner or admin can update teams" (lines 159-170). If the team name is being changed, checks for duplicates excluding itself via `Teams.org_id == team.org_id, Teams.team_name == data.team_name, Teams.team_id != team_id` — if found, raises 400 "Team name already exists in this organization" (lines 172-180). Sets `team.team_name = data.team_name` and `team.description = data.description`, commits, refreshes (lines 182-186). Logs the update (line 188). Returns `{"message": "Team updated successfully", "team_id": team.team_id, "team_name": team.team_name, "description": team.description, "org_id": team.org_id, "created_at": team.created_at}` (lines 190-197).

### Add Members to Team

`add_memebers_to_teams(team_id, data, user, db)` at line 199 extracts `user_id` (line 201). Queries `Teams` by ID — if not found, raises 404 "Team not found" (lines 203-206). Queries the org — if not found, raises 404 "Organization not found" (lines 208-211). Checks owner and admin — if neither, raises 403 "Only organization owner or admin can add members to teams" (lines 213-224). Looks up the target user by `data.user_id` — if not found, raises 404 "User not found" (lines 226-228). Checks if the target is the org owner or an org member via `Organization_members` — if neither, raises 400 "User must be a member of the organization first" (lines 230-240). Checks for existing `Team_association` — if already a member, raises 400 "User is already a member of this team" (lines 242-248). Creates a `Team_association(team_id=team_id, user_id=data.user_id)` (lines 250-254) and a `Team_roles` with all seven permission fields from the schema (lines 256-268). Adds both, commits, refreshes (lines 270-272). Looks up the added user's name for the log, calls `create_log` with action `"team_member_added"` and metadata including `team_id`, `team_name`, `role`, and `member_name` (lines 274-276). Returns `{"message": "Member added successfully", "user_id": data.user_id, "team_id": team_id, "role": new_role.role, "permissions": {"can_create_channels": new_role.can_create_channels, "can_send_messages": new_role.can_send_messages, "can_delete_messages": new_role.can_delete_messages, "can_manage_roles": new_role.can_manage_roles, "can_kick_members": new_role.can_kick_members, "can_make_announcement": new_role.can_make_announcement}}` (lines 278-291).

### Fetch Team Members

`fetch_team_members_service(team_id, user, db)` at line 293 extracts `user_id` (line 294). Queries `Teams` by ID — if not found, raises 404 "Team not found" (lines 296-299). Queries the org — if not found, raises 404 "Organization not found" (lines 301-304). Checks `is_owner` or org membership — if neither, raises 403 "You must be a member of this organization to view team members" (lines 306-313). Gets all `Team_association` rows for the team (lines 315-317). For each association, queries `Users` and `Team_roles` (lines 321-326). Builds a member data dict with `user_id`, `first_name`, `last_name`, `email`, `avatar_url`, `user_tag`, `phone_number`, `country`, `role` (falling back to `"MEMBER"` if no role record), and a `permissions` dict with all seven boolean fields (falling back to `False` per field if no role, or `None` for the entire permissions object if no role) (lines 328-348). Returns `{"team_id": team_id, "team_name": team.team_name, "members": members_list}` (lines 351-355).

### Update Member Permissions

`update_member_permissions_service(team_id, member_user_id, data, user, db)` at line 357 extracts `user_id` (line 358). Queries `Teams` — if not found, raises 404 "Team not found" (lines 360-363). Queries the org — if not found, raises 404 "Organization not found" (lines 365-368). Checks owner and admin (lines 370-375). Also queries the requester's `Team_roles` to check `can_manage_roles` (lines 377-382). If none of `is_owner`, `is_admin`, or `has_manage_permission` is true, raises 403 "You don't have permission to manage roles in this team" (lines 384-388). Verifies the target is a team member via `Team_association` — if not, raises 404 "User is not a member of this team" (lines 390-396). Verifies the target has a `Team_roles` record — if not, raises 404 "Member role not found" (lines 398-404). Captures `old_permissions` as a dict with `role` plus all seven booleans (lines 406-415). Applies all fields from `data.role`, `data.can_create_channels`, `data.can_send_messages`, `data.can_delete_messages`, `data.can_manage_roles`, `data.can_kick_members`, `data.can_make_announcement`, `data.can_manage_tasks` (lines 417-424). Commits and refreshes (lines 426-427). Captures `new_permissions` as the same dict shape (lines 429-438). Computes `changes = {k: {"from": old_permissions[k], "to": new_permissions[k]} for k in old_permissions if old_permissions[k] != new_permissions[k]}` (line 440). Looks up the target user's name (lines 442-443). Logs with action `"team_member_permissions_updated"` and metadata including `team_id`, `team_name`, `role`, `member_name`, and the `changes` dict (line 445). Returns `{"message": "Member permissions updated successfully", "user_id": member_user_id, "team_id": team_id, "role": member_role.role, "permissions": {"can_create_channels": member_role.can_create_channels, "can_send_messages": member_role.can_send_messages, "can_delete_messages": member_role.can_delete_messages, "can_manage_roles": member_role.can_manage_roles, "can_kick_members": member_role.can_kick_members, "can_make_announcement": member_role.can_make_announcement, "can_manage_tasks": member_role.can_manage_tasks}}` (lines 447-461).

### Kick Member

`kick_member_service(team_id, member_user_id, user, db)` at line 463 extracts `user_id` (line 464). Queries `Teams` — if not found, raises 404 "Team not found" (lines 466-469). Queries the org — if not found, raises 404 "Organization not found" (lines 471-474). Checks `is_owner = found_organization.owner_id == user_id` (line 476). Queries the requester's `Team_roles` to check `can_kick_members` (lines 479-484). If neither `is_owner` nor `has_kick_permission`, raises 403 "You don't have permission to kick members from this team" (lines 486-490). Checks if the target is the org owner — if so, raises 403 "Cannot kick organization owner" (lines 492-499). Verifies the target is a team member via `Team_association` — if not, raises 404 "User is not a member of this team" (lines 502-508). Looks up the target's `Team_roles` and `Users` for the log, calls `create_log` with action `"team_member_kicked"` and metadata including `team_id`, `team_name`, `member_name` (lines 510-517). If a `member_role` exists, deletes it; deletes the `target_member` association; commits (lines 519-523). Returns `{"message": "Member kicked successfully", "user_id": member_user_id, "team_id": team_id}` (lines 525-529).

### Fetch User Teams

`fetch_user_team_service(user, db)` at line 531 extracts `user_id` (line 533). Joins `Teams` with `Team_association` via `Teams.team_id == Team_association.team_id`, filtering by `Team_association.user_id == user_id`, and returns all matching teams (lines 535-539). Returns a list of `{"team_id", "team_name", "description", "org_id", "created_at"}` for each team (lines 541-550).

### Create Channels for Teams

`create_channels_for_teams_service(org_id, team_id, data, user, db)` at line 552 extracts `user_id` (line 554). Queries `Teams` — if not found, raises 404 "Team not found" (lines 556-559). If the team's org doesn't match the given `org_id`, raises 400 "Team does not belong to this organization" (lines 561-562). Queries the org — if not found, raises 404 "Organization not found" (lines 564-567). Checks `is_owner` and queries the requester's `Team_roles` for `can_create_channels` (lines 569-576). If neither, raises 403 "You don't have permission to create channels in this team" (lines 578-582). Checks for an existing channel with the same name in the team — if found, raises 400 "Channel name already exists in this team" (lines 584-590). Calls `get_channel_limit(found_organization.organization_plan)` — if the limit is not None and the current channel count for the org meets or exceeds the limit, raises 403 `f"Free plan allows a maximum of {channel_limit} channels. Upgrade to Pro for unlimited channels."` (lines 592-599). Creates a `Channels` row with `channel_name`, `channel_mode`, `channel_category`, `description`, `team_id`, `org_id`, adds, commits, refreshes (lines 601-612). Logs with action `"channel_created"` and metadata including `channel_name`, `team_id`, `team_name` (line 614). Returns `{"message": "Channel created successfully", "channel_id": new_channel.channel_id, "channel_name": new_channel.channel_name, "channel_mode": new_channel.channel_mode, "channel_category": new_channel.channel_category, "description": new_channel.description, "team_id": new_channel.team_id, "org_id": new_channel.org_id, "created_at": new_channel.created_at}` (lines 616-626).

### Fetch Channels for Teams

`fetch_channels_for_teams_service(org_id, team_id, user, db)` at line 628 extracts `user_id` (line 630). Queries `Teams` — if not found, raises 404 "Team not found" (lines 632-635). If team org doesn't match, raises 400 "Team does not belong to this organization" (lines 637-638). Queries the org — if not found, raises 404 "Organization not found" (lines 640-643). Checks `is_owner`, org membership, and team membership — if none, raises 403 "You don't have access to view channels in this team" (lines 645-661). Queries all `Channels` filtered by `team_id` (line 663). Returns a list of `{"channel_id", "channel_name", "channel_mode", "channel_category", "description", "team_id", "org_id", "created_at"}` (lines 665-677).

### Fetch Members Info

`fetch_members_info(org_id, team_id, target_user_id, user, db)` at line 679 extracts `user_id` (line 680). Queries `Organization` — if not found, raises 404 "Organization not found" (lines 682-685). Queries `Teams` — if not found, raises 404 "Team not found" (lines 687-690). If team org doesn't match, raises 400 "Team does not belong to this organization" (lines 692-693). Checks `is_owner`, admin membership, and team membership — if none, raises 403 "you are not allowed" (lines 695-709). Looks up the target user by ID — if not found, raises 404 "User not found" (lines 711-714). Checks `Team_association` for the target — if not a member, raises 404 "User is not a member of this team" (lines 716-722). Queries `Team_roles` for the target (lines 724-727). Returns `{"user": {"user_id": target_user.user_id, "first_name": target_user.first_name, "last_name": target_user.last_name, "email": target_user.email, "avatar_url": target_user.avatar_url, "user_tag": target_user.user_tag, "phone_number": target_user.phone_number, "country": target_user.country}, "organization_id": org_id, "team": {"team_id": team.team_id, "team_name": team.team_name, "role": target_role.role if target_role else "MEMBER", "permissions": {"can_create_channels": target_role.can_create_channels if target_role else False, "can_send_messages": target_role.can_send_messages if target_role else False, "can_delete_messages": target_role.can_delete_messages if target_role else False, "can_manage_roles": target_role.can_manage_roles if target_role else False, "can_kick_members": target_role.can_kick_members if target_role else False, "can_make_announcement": target_role.can_make_announcement if target_role else False}}}` (lines 729-754).

### Revoke Permissions from Team Members

`revoke_permissions_from_team_memebers(team_id, target_user_id, user, db, permission_name)` at line 757 extracts `requester_user_id = user.user_id` and casts `target_user_id = int(target_user_id)` (lines 758-759). Queries `Teams` — if not found, raises 404 "Team not found" (lines 761-763). Queries the org — if not found, raises 404 "Organization not found" (lines 765-767). Checks `is_owner` and requester's `Team_roles.can_manage_roles` — if neither, raises 403 "Only organization owner or members with manage roles permission can revoke permissions" (lines 769-782). Verifies the target is a team member — if not, raises 404 "User is not a member of this team" (lines 784-790). If the target is the org owner, raises 403 "Cannot revoke permissions from organization owner" (lines 792-793). Queries the target's `Team_roles` — if not found, raises 404 "Member role not found" (lines 795-801). Defines `permission_fields` as a set of 6 strings: `"can_create_channels"`, `"can_send_messages"`, `"can_delete_messages"`, `"can_manage_roles"`, `"can_kick_members"`, `"can_make_announcement"` (lines 803-810). If `permission_name` is provided, validates it against the set — if invalid, raises 400 "Invalid permission name" — then calls `setattr(target_role, permission_name, False)` for a single-permission revoke (lines 812-815). If `permission_name` is None (bulk revoke), sets `target_role.role = "MEMBER"` and all six booleans to `False` (lines 816-823). Commits and refreshes (lines 825-826). Looks up the target user's name, logs with action `"team_member_permissions_revoked"` and metadata including `permission: permission_name if permission_name else "all"` (lines 828-831). Returns `{"message": "Member permissions revoked successfully", "user_id": target_user_id, "team_id": team_id, "role": target_role.role, "permissions": {"can_create_channels": target_role.can_create_channels, "can_send_messages": target_role.can_send_messages, "can_delete_messages": target_role.can_delete_messages, "can_manage_roles": target_role.can_manage_roles, "can_kick_members": target_role.can_kick_members, "can_make_announcement": target_role.can_make_announcement}}` (lines 833-846).

### Fetch Files for Team Channel

`fetch_files_for_team_channel_service(org_id, team_id, channel_id, user, db)` at line 848 extracts `user_id` (line 855). Queries `Teams` — if not found, raises 404 "Team not found" (lines 857-859). If team org doesn't match, raises 400 "Team does not belong to this organization" (lines 861-862). Queries `Channels` filtered by `channel_id`, `org_id`, `team_id` — if not found, raises 404 "Team channel not found" (lines 864-871). Queries the org — if not found, raises 404 "Organization not found" (lines 873-875). Checks `is_owner`, org admin, and team membership — if none, raises 403 "You don't have access to this team channel" (lines 877-889). Joins `Files` with `Users` on `Files.sender_id == Users.user_id`, filtered by `Files.team_id == team_id`, `Files.channel_id == channel_id`, `Files.is_deleted == False`, ordered by `Files.sent_at.asc()` (lines 891-897). Returns `{"org_id": org_id, "team_id": team_id, "channel_id": channel_id, "files": [{"id": file_record.id, "file_name": file_record.file_name, "file_url": file_record.file_url, "file_size": file_record.file_size, "sent_at": file_record.sent_at, "sender": {"user_id": sender.user_id, "first_name": sender.first_name, "last_name": sender.last_name, "avatar_url": sender.avatar_url, "user_tag": sender.user_tag}} for file_record, sender in files]}` (lines 899-920).

### View PDF

`view_pdf(org_id, team_id, file_id, user, db)` at line 923 extracts `user_id` (line 924). Queries `Teams` — if not found, raises 404 "Team not found" (lines 926-928). If team org doesn't match, raises 400 "Team does not belong to this organization" (lines 930-931). Queries the org — if not found, raises 404 "Organization not found" (lines 933-935). Checks `is_owner`, org admin, and team membership — if none, raises 403 "You don't have access to this file" (lines 937-949). Queries `Files` filtered by `id == file_id`, `team_id == team_id`, `is_deleted == False` — if not found, raises 404 "File not found" (lines 951-958). Imports `os` and `urllib.parse.quote` (lines 960-961). Checks the file extension via `os.path.splitext(file.file_name)[1].lower() != ".pdf"` — if not PDF, raises 400 "Only PDF files can be viewed inline" (lines 963-964). Creates an `httpx.Client(timeout=30.0, follow_redirects=True)` (line 966). Attempts to open an upstream streaming GET request to `file.file_url` via `client.stream("GET", file.file_url).__enter__()` — on `httpx.HTTPError` closes the client and raises 502 "Failed to fetch file from storage" (lines 967-971). If the upstream response status is not 200, closes upstream and client, raises 502 "Failed to fetch file from storage" (lines 973-976). Captures `content_length = upstream.headers.get("content-length")` (line 978). Defines an inner `iterator()` that yields chunks of 64KB via `upstream.iter_bytes(chunk_size=64 * 1024)` and ensures both upstream and client are closed in the `finally` block (lines 980-986). Builds response headers: `quoted_file_name = quote(file.file_name)`, `safe_file_name = file.file_name.replace('"', "")`, then `"Content-Disposition"` set to `f'inline; filename="{safe_file_name}"; filename*=UTF-8\'\'{quoted_file_name}'`, `"X-Content-Type-Options": "nosniff"`, and conditionally `"Content-Length"` if `content_length` is available (lines 988-995). Returns `StreamingResponse(iterator(), media_type="application/pdf", headers=response_headers)` (lines 997-1001).
