# Admin Flow

## Admin Router

`backend/routers/admin_router.py:17` defines `router = APIRouter()`.

`_require_admin(user)` at line 20 checks `if user.role != "admin"` and raises `HTTPException(status_code=403, detail="Admin access required")` (lines 20-23).

`GET /admin/overview` at line 27 calls `_require_admin(user)`, then computes seven aggregate counts (lines 31-55). `users_count` is `db.query(func.count(Users.user_id)).scalar() or 0` (line 33). `active_users_count` filters by `Users.status != "offline"` (line 34). `orgs_count` counts organizations (line 35). `paid_orgs_count` filters by `func.upper(func.coalesce(Organization.organization_plan, "FREE")) == "PRO"` (lines 36-38). `free_orgs_count = max(0, orgs_count - paid_orgs_count)` (line 39). `channels_count` counts channels (line 40). `messages_sent` sums three counts: `channel_msgs` from `Messages`, `direct_msgs` from `Direct_messages`, and `group_msgs` from `Group_chat_messages` (lines 42-45). Returns all seven as `users_count`, `active_users_count`, `organizations_count`, `paid_orgs_count`, `free_orgs_count`, `channels_count`, `messages_sent` (lines 47-55).

`GET /admin/users` at line 59 calls `_require_admin(user)` (line 63). Queries all `Users` ordered by `Users.joined_at.desc()` (line 65). Returns a list of dicts with `user_id`, `first_name`, `last_name`, `email`, `avatar_url`, `country`, `user_tag`, `is_verified`, `profile_completed`, `status`, `role`, `account_status`, `joined_at.isoformat()`, and `last_login_at.isoformat()` (lines 67-84).

`POST /admin/users/{target_user_id}/ban` at line 88 calls `_require_admin(user)` (line 93). If `target_user_id == user.user_id`, raises `HTTPException(400, "You cannot ban yourself")` (lines 94-95). Looks up the target user — if not found, raises `HTTPException(404, "User not found")` (lines 97-99). If `target.role == "admin"`, raises `HTTPException(400, "Cannot ban another administrator")` (lines 100-101). Sets `target.account_status = "banned"` (line 103). Revokes all refresh tokens via `db.query(Refresh_tokens).filter(Refresh_tokens.user_id == target_user_id, Refresh_tokens.revoked_at.is_(None)).update({Refresh_tokens.revoked_at: now})` with `synchronize_session=False`, where `now = datetime.now(UTC)` (lines 105-109). Commits, returns `{"message": "User banned", "user_id": target_user_id, "account_status": target.account_status}` (lines 111-112).

`POST /admin/users/{target_user_id}/unban` at line 116 calls `_require_admin(user)` (line 121). Looks up the target — if not found, raises `HTTPException(404, "User not found")` (lines 122-124). Sets `target.account_status = "active"` (line 126). Commits, returns `{"message": "User unbanned", "user_id": target_user_id, "account_status": target.account_status}` (lines 127-128).

`DELETE /admin/organizations/{org_id}` at line 132 calls `_require_admin(user)` (line 137). Looks up the organization — if not found, raises `HTTPException(404, "Organization not found")` (lines 138-140). Deletes via `db.delete(org)`, commits, returns `{"message": "Organization deleted", "organization_id": org_id}` (lines 142-144).

`GET /admin/organizations` at line 148 calls `_require_admin(user)` (line 152). Fetches all organizations ordered by `created_at.desc()` (line 154). Builds `org_ids` list (line 155). Fetches all teams (lines 157-158), channels (lines 160-162), and members joined with `Users` (lines 163-170) filtered by `org_ids`. Builds an `owners` dict keyed by `user_id` from a query of all org owners (lines 171-178). Groups teams by org into `teams_by_org` (lines 180-182). Groups channels by team into `channels_by_team` and org-level channels into `channels_by_org_root` (lines 184-190). Groups members by org into `members_by_org` (lines 192-201). For each org, builds a result node with `organization_id`, `organization_name`, `organization_picture`, `organization_tag`, `organization_plan`, `created_at`, `owner` (owner user info with `user_id`, `first_name`, `last_name`, `email`, `avatar_url`), `members_count`, `teams_count`, `channels_count`, `members` list, `teams` list (each with nested `channels` array containing `channel_id`, `channel_name`, `channel_category`), and `org_channels` list (org-level channels, lines 203-255). Returns the full `result` list (line 257).
